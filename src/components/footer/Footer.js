import styles from './Footer.module.scss';
import "@fontsource/vt323"; // Defaults to weight 400

export default function Footer(){
    return (
        <div className={styles.footer}>
            all rights reserved @ sasathepajic
        </div>
    )
}